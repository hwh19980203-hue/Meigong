# 知识库模式文档

> 本知识库的三层架构和完整工作流定义在 **[[AGENTS.md]]** 中。本文档仅作为入口页，所有 LLM 会话请以 `AGENTS.md` 为唯一模式标准。

## 一句话原则

```
raw/    ← 原始资料（只读，你存放的地方）
wiki/   ← Wiki 知识库（LLM 维护，跨会话积累）
AGENTS.md  ← 模式标准（本文档的上级）
```

## 在 Claude 中的使用

1. 所有工作流定义、目录约定、命名规范、作图接口约定 → 见 [[AGENTS.md]]
2. 在开始需要**操作知识库**（摄入、作图、更新索引和日志）的会话前，先读 `AGENTS.md`
3. 用户提问时 → 先读 `wiki/index.md` 定位页面，再深入阅读
4. 每次操作后 → 更新 `wiki/index.md` 和 `wiki/log.md`

## 不变原则

1. **永不修改原始资料**——`raw/` 下的文件只读
2. **永不删除用户文件**——除非明确要求
3. **保持一致**——更新索引和日志是所有操作的固定步骤
4. **增量构建**——每次只处理一个，循序渐进
