# 知识库模式文档 (Knowledge Base Schema)

> 本文档定义了 LLM 如何维护本知识库。所有 LLM 会话都应该遵循本文档的约定。

## 架构总览

本知识库采用 [[LLM]] 文档描述的三层架构：

```
raw/             ← 原始资料（不可修改，由用户维护）
wiki/            ← LLM 生成的 Wiki 页面（LLM 完全维护，用户阅读）
AGENTS.md        ← 模式文档（本文档，用户与 LLM 共同进化）
```

## 目录结构

```
/
├── AGENTS.md            ← 本文档 — LLM 的工作指令
├── LLM.md               ← 原始设计文档
├── 欢迎.md               ← 欢迎页面
│
├── raw/                 ← 原始资料（用户存放，LLM 只读）
│   ├── _README.md       ← 使用说明
│   ├── assets/          ← 本地化图片/附件
│   ├── articles/        ← 文章
│   ├── books/           ← 书籍
│   ├── podcasts/        ← 播客
│   ├── videos/          ← 视频
│   ├── papers/          ← 论文
│   ├── images/          ← 截图
│   └── sources/         ← 其他外部知识库快照
│
└── wiki/                ← Wiki 知识库（LLM 维护）
    ├── index.md         ← 内容索引（LLM 维护）
    ├── log.md           ← 操作日志（LLM 维护）
    ├── entities/        ← 实体页面（人物、组织、作品等）
    ├── concepts/        ← 概念页面（理论、方法论、模式等）
    ├── standards/       ← 规范标准页面（主图规范、A+规范等）
    ├── workflows/       ← 工作流页面（SOP、操作流程等）
    ├── templates/       ← 模板页面（PSD模板、A+模板、输入表等）
    ├── cases/           ← 案例页面（优秀案例、失败案例、复盘等）
    ├── products/        ← 产品知识页面（产品卖点、视觉方向、素材说明等）
    ├── analyses/        ← 专题分析/健康检查/对比报告
    └── sources/         ← 资料摘要（每份原始资料的摘要页面）
```

## 工作流

### 1. 摄入 (Ingest)

当用户说"处理这份资料"或"帮我读一下 X"时：

1. **读取**原始资料（从 `raw/` 或指定路径），同时读取资料中嵌入的图片文件以获取完整上下文
2. **与用户讨论**：确认重点、澄清疑问、了解上下文
3. **创建摘要页面**：在 `wiki/sources/` 下创建资料摘要
4. **更新实体页面**：在 `wiki/entities/` 中创建或更新相关实体
5. **更新概念页面**：在 `wiki/concepts/` 中创建或更新相关概念
6. **更新规范页面**：如涉及设计标准，在 `wiki/standards/` 中创建或更新规范页面
7. **更新模板页面**：如涉及公司模板、作图输入表、提示词模板，在 `wiki/templates/` 中创建或更新页面
8. **更新案例页面**：如涉及历史设计、优秀图、失败案例、复盘，在 `wiki/cases/` 中创建或更新页面
9. **更新产品页面**：如涉及具体产品、ASIN/SKU、产品卖点，在 `wiki/products/` 中创建或更新页面
10. **更新索引**：在 `wiki/index.md` 中添加/更新条目
11. **记录日志**：在 `wiki/log.md` 追加一条记录

**资料摘要页面的格式建议：**
```yaml
---
title: "资料标题"
source: "来源链接或出处"
date: 2026-06-07
type: article | book | podcast | video | paper
tags: [相关标签]
---
```

**单次摄入可能影响的页面数：** 5-15 个页面（取决于资料的丰富程度）。

### 2. 查询 (Query)

当用户提问时：

1. 先读 `wiki/index.md` 定位相关页面
2. 深入阅读相关页面内容
3. **读取页面中嵌入的图片**：当笔记中出现 `![[path/to/image]]` 时，使用 Read 工具读取对应图片文件，图文结合理解（尤其涉及产品外观、设计稿、质检对比时）
4. 综合回答，**引用来源**（使用 `[[wiki/sources/xxx]]` 或 `[[wiki/entities/xxx]]`）
5. **好的回答本身可以沉淀到 Wiki 中**——如果回答有长期价值，在 `wiki/analyses/`、`wiki/workflows/`、`wiki/templates/` 等合适目录下创建新页面，并更新索引和日志

### 3. 检查/维护 (Lint)

当用户说"检查知识库"或"健康检查"时：

1. 检查页面间的矛盾
2. 检查被新资料替代的过时说法
3. 检查孤儿页面（没有入链的页面）
4. 检查缺乏页面的重要概念
5. 检查缺失的交叉引用
6. 提出可以通过网络搜索填补的数据缺口
7. 记录结果到 `wiki/log.md`

## 页面格式约定

- **文件名**：使用有意义的名称，如 `思维导图方法论.md`
- **Wiki 链接**：内部链接使用 `[[wiki/entities/xxx]]` 或 `[[wiki/concepts/xxx]]` 格式
- **标签**：在 YAML frontmatter 中使用 `tags:` 字段
- **图片**：引用本地图片使用以下路径约定——
  - 产品实拍图 → `![[raw/cases/[编号]-[人名]/[文件名]]]`（如 `![[raw/cases/786-马艳丽/786-real-1.jpg]]`），这是产品外观的**唯一准确依据**
  - AI 生成资产 → `![[raw/assets/generated/[文件名]]]`（如 `![[raw/assets/generated/786-main-image-v6.png]]`）
  - 其他资料图片 → `![[raw/assets/[文件名]]]`
- **交叉引用**：页面底部添加"参见"部分，列出相关页面链接

## 命名规范

- `wiki/entities/` 页面：实体名称（人物用中文全名，如 `李开复.md`）
- `wiki/concepts/` 页面：概念名称（如 `第二大脑方法.md`）
- `wiki/sources/` 页面：`[日期]-[简短标题].md`（如 `2026-06-07-知识库设计模式.md`）
- `wiki/standards/` 页面：规范名称（如 `亚马逊主图规范.md`）
- `wiki/workflows/` 页面：流程名称（如 `Codex调用GPT作图工作流.md`）
- `wiki/templates/` 页面：模板名称（如 `AI作图任务标准输入表.md`）
- `wiki/cases/` 页面：`[日期]-[产品/项目]-[案例类型].md`
- `wiki/products/` 页面：`[编号]-[产品名].md` 或 `[ASIN]-[产品名].md`
- `wiki/analyses/` 页面：`[日期]-[分析主题].md`
- `raw/` 下文件：保持原始文件名，或使用 `[日期]-[来源]-[标题].md`

## 部门字段约定

产品页、案例页和作图输入表都应保留 **所属店铺** 字段。不同店铺可能存在不同品牌调性、色彩偏好、字体体系、构图习惯和模板要求；当店铺未知时先写“待补充”，不要省略该字段。

## 作图接口约定

本知识库已接入 otuapi 图片生成接口，供 Codex 在黑伞美工工作流中调用。

> **核心原则：产品外观以实拍图为准。** 任何时候调用外部 API 生成图片，都必须从 `raw/cases/[编号]-[人名]/` 中读取实拍图作为产品外观的唯一依据。AI 模型输出与实拍图不一致时，以实拍图为准进行修改或重做，不得让 AI 凭文字描述自由发挥产品形状、颜色、纹理、拉链方向等物理特征。
>
> **图片读取说明：** 笔记中的 `![[path/to/image]]` 只是文本链接，Claude 不会自动渲染图片。读取产品页或案例页时，必须使用 Read 工具逐一读取笔记中引用的实拍图文件（.jpg/.png），才能让多模态能力实际"看到"产品外观。详见 [[wiki/concepts/图片管理与LLM配合指南]]。

- 首选工具：当用户要求“作图 / 生图 / 生成主图 / 生成 A+ 图 / 参考图改图 / 测试 otuapi”时，优先调用 MCP 工具 `otuapi_image`。
- 可用能力：`generate_image` 用于文生图或带参考图生成（image2 和 Banana 模型均通过 `images` 参数支持参考图），`edit_image` 用于参考图改图。
- 默认模型：`image2`；如用户指定 `nano_banana_2` 或 `nano_banana_pro-*`，MCP 工具应通过 `model` 参数传入模型名并走 `POST /v1/videos` 异步任务接口，再轮询 `GET /v1/videos/{task_id}` 下载结果；如用户指定 `gemini-3-pro-image-preview` 或 `gemini-3.1-flash-image-preview`，走 `v1beta/models/{model}:generateContent` 同步接口。
- 默认尺寸：按任务选择 `1024x1024`、`1024x1792` 或 `1792x1024`；未指定时先用 `1024x1024`。
- 生成资产目录：`raw/assets/generated/`，保存 API 原图、修改版本、总览图和知识库长期引用资产；不要写入 `raw/` 下其他原始资料目录。
- 正式交付目录：`output/imagegen/[编号-产品名]/`，只保存质检通过、已按交付尺寸和名称整理的终版图片，不保存 API 原图、失败版本或过程图。
- 引用原则：Wiki 产品页和案例页继续引用 `raw/assets/generated/` 中的稳定资产；`output/imagegen/` 作为给运营、美工或开发直接取用的交付目录，避免整理或重命名交付文件时破坏知识库图片链接。
- 密钥管理：密钥只保存在用户环境变量或 `C:\Users\Q\.codex\.env` 的 `OTUAPI_API_KEY`，不要写入 Wiki、案例、产品页或提交到仓库。
- 生成后处理顺序：先保存生成资产到 `raw/assets/generated/`，再质检和修改，随后把终版复制到 `output/imagegen/[编号-产品名]/`，最后更新产品页、案例页、索引和日志。
- 产品母版锁定：已有用户确认的首图、白底图或实拍图时，后续 A+、场景图、广告图必须复用同一产品主体，不得让 AI 整图重绘产品。优先采用“固定产品抠图 + AI 无产品背景 + 分层合成”；尺寸图、材质图和步骤图使用同一母版或其裁切。
- 产品一致性硬门禁：交付前必须并排对照母版，检查外轮廓、长宽比例、边缘、尖角、孔位、材质层次、文字/图案造型和连接方式。即使数量、文字、场景正确，只要产品外形漂移就判定失败，不得写入 `output/imagegen/`。
- **参考图强制传递（所有模型）**：调用 `generate_image` 时，无论使用 `image2`、`nano_banana_2`、`nano_banana_pro-*` 还是 `gemini-*` 模型，都必须从产品页或案例页读取实拍图/白底图/首图，通过 `images` 参数传入 API 作为参考图（最多 10 张）。严禁仅靠文字描述让 AI 想象产品外观——所有模型后端均已支持参考图输入，Codex 必须始终使用此能力。实拍图路径可从产品页的"实拍参考图"或原始资料目录获取。

## 不变原则

1. **永不修改原始资料**——`raw/` 下的文件只读
2. **永不删除用户文件**——除非明确要求
3. **保持一致**——更新索引和日志是所有操作的固定步骤
4. **增量构建**——每次只处理一个资料，循序渐进
